import Link from "next/link";import {getJSON,Incident} from "@/lib/api";
export default async function Incidents(){let rows:Incident[]=[];try{rows=await getJSON("/api/incidents")}catch{}return <><header className="header"><div><span className="eyebrow">Incident operations</span><h1>Incidents</h1><p>Search, triage, and investigate operational failures.</p></div><Link className="button" href="/incidents/new">New incident</Link></header><section className="card"><div className="filters"><input className="input" placeholder="Search incident, service, or ID"/><select className="input" style={{maxWidth:160}} defaultValue=""><option value="">All severities</option><option>SEV-1</option><option>SEV-2</option><option>SEV-3</option><option>SEV-4</option></select></div><table className="table"><thead><tr><th>Incident</th><th>Severity</th><th>Service</th><th>Status</th><th>Investigator</th><th>Created</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><Link href={`/incidents/${x.id}`}><strong>{x.title}</strong><div className="sub">{x.id} · {x.environment}</div></Link></td><td><span className={`badge ${x.severity.toLowerCase()}`}>{x.severity}</span></td><td>{x.service}</td><td><span className="badge status">{x.status}</span><div className="sub">{x.ai_state}</div></td><td>{x.investigator}</td><td className="sub">{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">The API is unavailable. Start the backend on port 8000.</div>}</section></>}
