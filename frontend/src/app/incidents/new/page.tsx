"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {postJSON,Incident} from "@/lib/api";
const demos=["Payment API Failure","Redis Outage","ML Inference Timeout","Deployment Regression","Database Pool Exhaustion"];
export default function NewIncident(){const router=useRouter();const [form,setForm]=useState({title:"Payment API returning 500 responses",description:"Errors began immediately after the latest deployment and affect checkout requests.",service:"payment-api",environment:"production",severity:"SEV-1"});const submit=async()=>{const i=await postJSON<Incident>("/api/incidents",form);router.push(`/incidents/${i.id}`)};return <><header><span className="eyebrow">Incident intake</span><h1>Start an AI investigation</h1><p>Describe the symptoms or load a seeded scenario for a guided demonstration.</p></header><div className="split" style={{marginTop:25}}><section className="card stack"><label className="sub">Title<input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label className="sub">Description<textarea className="input" rows={6} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="grid" style={{gridTemplateColumns:"1fr 1fr"}}><label className="sub">Service<select className="input" value={form.service} onChange={e=>setForm({...form,service:e.target.value})}><option>payment-api</option><option>auth-service</option><option>redis-cache</option><option>ml-inference-api</option></select></label><label className="sub">Severity<select className="input" value={form.severity} onChange={e=>setForm({...form,severity:e.target.value})}><option>SEV-1</option><option>SEV-2</option><option>SEV-3</option><option>SEV-4</option></select></label></div><button className="button" onClick={submit}>Start AI investigation</button></section><aside className="card"><div className="section-head"><h2>Load demo incident</h2></div>{demos.map((x,i)=><button className="button secondary" style={{width:"100%",textAlign:"left",marginBottom:8}} key={x} onClick={()=>setForm({...form,title:x,service:i===1?"redis-cache":i===2?"ml-inference-api":"payment-api"})}>{x}</button>)}</aside></div></>}
